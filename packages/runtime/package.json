{
  "name": "@desktop-proxy/runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Main process runtime — hooks Electron sessions, manages plugins, provides IPC bridge",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@desktop-proxy/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "electron": "^42.3.1",
    "typescript": "^6.0"
  }
}
