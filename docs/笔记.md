





* Network interceptor 还需要有electron中主进程的拦截器
* 像windsurf、cursor等可能都会启动多个窗口。当前的方式是否会在多窗口模式下，导致多窗口冲突？



> 听说claude使用其他模型，会往请求里添加随机内容，降低缓存命中率？先确认是否存在，然后我们的dprox进行修复这个问题





```shell
/Volumes/fushilu/.nvm/versions/node/v22.22.1/bin/node \
  /Users/fushilu/workspace/revocloud/desktop-proxy/packages/installer/dist/cli.js \
  relay on --codex \
  --upstream https://api.deepseek.com/v1 \
  --key sk-a4dd372ee8f04f5cb018ecb4034a2014 \
  --wire-api chat \
  --map "gpt-*=deepseek-v4-flash"
```







* 框架管理页（B）——立刻可用、价值高、纯渲染层，把已有的 toggle/safe-mode/log-level/stealth 用 UI 暴露出来。
* 自动修复 watcher（A）——解决「app 一更新就失效」的最大痛点。
* 插件脚手架 + CI（C）——降低插件作者门槛 + 防回归。
* 之后再考虑权限模型、`api.ui`、版本校验、purge 等。





> 需要一个请求分析功能，分析出哪些请求是做什么的，有点类似于mitmproxy，只是我们的项目是在Electron里，不需要解密tls、ca证书之类的。感觉有点类似于devtools的network面板的功能，只是包含到了主进程和渲染进程。如果要设计UI需要配置一个好看的UI skill才行，你推荐一下